import { Text, SearchInput } from './UI';

const SearchContainer = () => {
  return (
    <>
      <SearchInput type='text' placeholder='Search for user' />
      <Text></Text>
    </>
  );
};

export default SearchContainer;
